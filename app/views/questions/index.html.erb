<h1>All Questions</h1>
<% @questions.each do |q| %>
  <%= image_tag q.image.url(:thumb), class: 'question-thumbnail' %>
  <%= link_to q.title, question_path(q) %>
  <hr>
<% end %>
<div id='paginator'><%= paginate @questions %></div>
