<div class="row">
  <div class="col-xs-12 col-sm-8 col-sm-offset-1">
    <h1>Contact Us</h1>

<%# form_tag is helpber method that will generate a form HTML element
with POST http request.  The first argument is the url you would like
 to submit the form to.%>

    <%= form_tag contact_submit_path do %>
    <div class="form-group">
      <%= label_tag :full_name %>
      <%= text_field_tag :full_name, nil,  class: "form-control"  %>
    </div>
    <div class="form-group">
      <%= label_tag :email %>
      <%= email_field_tag :email, nil,  class: "form-control"  %>
    </div>
    <div class="form-group">
      <%= label_tag :message %>
      <%= text_area_tag :message, nil,  class: "form-control" %>
    </div>

      <%= submit_tag "Contact Us!", class: "btn btn-default" %>
    <% end %>
  </div>
</div>
